# =============================================================================
# requirements.txt
# Mutual Fund FAQ Assistant — Python dependencies
# =============================================================================

# ---- Config & Validation (Phase 0) -----------------------------------------
pyyaml>=6.0.1
pydantic>=2.7.0

# ---- Ingestion Pipeline (Phase 1) ------------------------------------------
requests>=2.32.0
httpx>=0.27.0                          # async HTTP client
beautifulsoup4>=4.12.3
lxml>=5.2.2

# ---- Embedding (Phase 1.5) -------------------------------------------------
sentence-transformers>=3.0.0           # bge-small-en (primary embedder)
torch>=2.3.0                           # required by sentence-transformers

# ---- Indexing (Phase 1.6) --------------------------------------------------
faiss-cpu>=1.8.0                       # FAISS dense index
rank-bm25>=0.2.2                       # BM25 sparse index

# ---- Retrieval / Re-ranking (Phase 2) --------------------------------------
# cross-encoder/ms-marco-MiniLM-L-6-v2 loaded via sentence-transformers

# ---- LLM Client (Phase 3) --------------------------------------------------
groq>=0.9.0                            # Groq OpenAI-compatible client
openai>=1.30.0                         # Optional: text-embedding-3-small fallback

# ---- Backend API (Phase 4) -------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ---- Testing & CI (Phase 5) ------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                          # FastAPI test client

# ---- Utilities -------------------------------------------------------------
python-dotenv>=1.0.1                   # Load .env files for local dev
